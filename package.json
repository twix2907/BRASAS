{
  "scripts": {
    "build": "cd resources/js && npm install && npm run build && cd ../..",
    "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
  },
  "engines": {
    "node": "20.x",
    "php": "8.2.x"
  }
}