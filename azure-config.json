{
  "name": "dbrasas-restaurant-app",
  "version": "1.0.0",
  "description": "Sistema de gestión para restaurante D'Brasas y Carbón",
  "main": "public/index.php",
  "scripts": {
    "azure:deploy": "npm install && npm run build && composer install --no-dev --optimize-autoloader",
    "azure:postdeploy": "php artisan config:cache && php artisan route:cache && php artisan view:cache"
  },
  "azure": {
    "php": {
      "version": "8.2"
    },
    "node": {
      "version": "18"
    },
    "webSiteConfig": {
      "phpVersion": "8.2",
      "nodeVersion": "18-lts",
      "documentRoot": "public",
      "defaultDocuments": ["index.php"]
    }
  }
}
